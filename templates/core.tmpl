// Code generated; DO NOT EDIT.

package xodr

{{range .SimpleType}}
    type {{ toCamel .Name }} struct {
    }
{{end}}

{{range .ComplexType}}
    // {{ formatStructDocumentation .Annotation.Documentation.Text }}
    type {{ distinctSignalsReferenceToCamel (removeTypePrefix .Name) }} struct {
        {{$base := .ComplexContent.Extension.Base}}
        {{toCamel (removeTypePrefix $base)}}

        {{- range .ComplexContent.Extension.Sequence.Element}}
        {{- if ne $base  .Type}}
        {{toCamel .Name}} {{mapPrimitives (distinctSignalsReferenceToCamel (removeTypePrefix .Type))}}
        {{- end}}
        {{- end}}

        {{- range .ComplexContent.Extension.Attribute}}
        {{- if ne $base (toCamel .Type)}}
        {{toCamel .Name}} {{mapPrimitives .Type}}
        {{- end }}
        {{- end }}

    }
{{end}}
